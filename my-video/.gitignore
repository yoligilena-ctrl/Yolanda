node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Tu video real subido a public/ (contenido personal): se queda local para
# que sigas iterando, pero nunca se commitea/sube al repo sin que lo pidas.
public/mi-video.webm
public/mi-video.cameramoves.json
